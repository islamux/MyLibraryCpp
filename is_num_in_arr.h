#include <iostream>
using namespace std;

bool IsNumInArr(int numToCheck){

    // My solution 
    // if(numToCheck != -1)
    //     return true;
    
    // return false;

    // BY abu Hadhud with some changes by using variable not the function itself.
    return numToCheck != -1;

}